[project]
name = "ww-tests"
version = "0.1.0"
description = "Tests for ww.exe - Windows workspace manager"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8",
    "pywin32>=308",
    "psutil>=6",
]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["."]
